import * as actions from './actions';

// Forwarded action creators
// ...

// Thunks, Sagas, etc.
export function sendRequest(data) {
  return (dispatch, getState) => {
    // return dispatch(actions.doSomething());
    return;
  };
}
